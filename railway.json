{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install && DATABASE_URL=file:./dev.db npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd server && if [ ! -f prisma/dev.db ]; then echo 'No DB found, creating...' && DATABASE_URL=file:./dev.db npx prisma db push --accept-data-loss && DATABASE_URL=file:./dev.db node prisma/seed.js; else echo 'DB exists, starting...'; fi && DATABASE_URL=file:./dev.db node src/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
