{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd server && DATABASE_URL=file:./prisma/dev.db npx prisma migrate deploy && DATABASE_URL=file:./prisma/dev.db node src/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
